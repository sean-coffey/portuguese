version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  command: python3 -m uvicorn app.api:app --host 0.0.0.0 --port 8000
  network:
    port: 8000